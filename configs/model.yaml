name: "nafnet_lite"

in_channels: 1
out_channels: 1
width: 32
enc_blocks: [2, 2, 4]
middle_blocks: 4
dec_blocks: [2, 2, 2]

# Set this after dataset inspection if GT is a fixed multiple of input.
# 1 means restoration at the same spatial size.
scale: 2
